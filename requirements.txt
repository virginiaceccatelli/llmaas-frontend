# Pinned to the same versions the broker uses, so the two tiers cannot drift.
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
pydantic==2.10.4
pydantic-settings==2.7.0
PyJWT==2.10.1
redis==5.2.1
